spring:
  application:
    name: registry

server:
  port: 8081

eureka:
  client:
    resgister-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:${server.port}/eureka/
